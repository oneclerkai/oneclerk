Let’s systematically isolate and fix why your bots are stalling, refusing to switch languages or voices, and completely failing to read your drag-and-drop canvas setup.

The core reason your application is experiencing these issues simultaneously comes down to a **schema format mismatch** and a lack of **pre-flight event handling**. If Vapi receives an override format it does not perfectly understand, or if it tries to initialize audio streams before the browser fully initializes your microphone, it will crash silently or freeze on the "Connecting..." state.

Here is the exact architectural blueprint and clean, copy-paste JavaScript logic to completely fix both files in your Replit workspace.

---

## 🛠️ Why Your Current Setup is Failing

* **The Stalling Connection:** Your code is likely running `vapi.start()` before the browser has completely resolved the user's microphone permission. This creates an asynchronous WebRTC queue jam.
* **The Static Voice/Language:** By default, Vapi targets its base English model. If you pass an override to change the language to Hindi or Spanish without explicitly defining the underlying engine model as **`sonic-multilingual`** for Cartesia, the network will completely ignore your voice selection or lag heavily attempting to process translation.
* **The Broken Canvas Link:** The "Play Sample" button doesn't automatically know what nodes were dragged onto the screen. It needs a scraper script to check the active canvas container fields before launching.

---

## 💻 1. The Core Fix: Public Homepage Script (`app.js`)

Replace your landing page's Vapi execution block with this streamlined logic. It creates explicit mapping tables and forces the browser to request mic permission **before** initiating the handshake.

```javascript
// 1. Initialize Vapi safely with your Public Token
const vapi = new Vapi("bace6e2b-19b8-403f-84aa-7c9b8ae0dea8");

// 2. Strict ID mapping matrix to keep Vapi engines stable
const HARKLY_LANGUAGES = {
    "en": { transcriber: "en", voiceLang: "en" },
    "hi": { transcriber: "hi", voiceLang: "hi" },
    "es": { transcriber: "es", voiceLang: "es" },
    "fr": { transcriber: "fr", voiceLang: "fr" }
};

const HARKLY_VOICES = {
    "Maya": "244f6fbf-8afb-47e2-8958-3d1487216a90",
    "Arjun": "57dcab65-68ac-45a6-8480-6c4c52ec1cd1"
};

const homeCallBtn = document.querySelector('#speak-to-agent-btn'); // Double check this matches your HTML ID

if (homeCallBtn) {
    homeCallBtn.addEventListener('click', async () => {
        try {
            // STEP A: Force instant microphone popup to stop connection lag
            homeCallBtn.innerText = "Requesting Mic...";
            await navigator.mediaDevices.getUserMedia({ audio: true });

            // STEP B: Read active UI choices dynamically right at the millisecond of the click
            const selectedLangRaw = document.querySelector('#language-select')?.value || "en";
            const selectedVoiceRaw = document.querySelector('#voice-select')?.value || "Maya";

            const langConfig = HARKLY_LANGUAGES[selectedLangRaw] || HARKLY_LANGUAGES["en"];
            const voiceId = HARKLY_VOICES[selectedVoiceRaw] || HARKLY_VOICES["Maya"];

            // STEP C: Construct the precise nested JSON schema Vapi requires to avoid 400 Bad Requests
            const assistantOverrides = {
                transcriber: {
                    provider: "deepgram",
                    model: "nova-2",
                    language: langConfig.transcriber
                },
                voice: {
                    provider: "cartesia",
                    model: "sonic-multilingual", // Mandatory key for multi-language support
                    voiceId: voiceId,
                    language: langConfig.voiceLang
                }
            };

            console.log("[Harkly Sync] Initializing Home Bot with payload:", JSON.stringify(assistantOverrides));
            homeCallBtn.innerText = "Connecting...";
            
            // Launch call with Homepage Assistant ID
            vapi.start("5b54d785-e86b-420e-ace0-26092882287e", assistantOverrides);

        } catch (error) {
            console.error("[Harkly Error] Core setup failed:", error.message);
            homeCallBtn.innerText = "Speak to Agent";
            alert("Microphone permission or token initialization error.");
        }
    });
}

// 3. Keep UI status elements perfectly synced with WebRTC stream status
vapi.on('call-start', () => { homeCallBtn.innerText = "Stop Call"; });
vapi.on('call-end', () => { homeCallBtn.innerText = "Speak to Agent"; });

```

---

## 💻 2. The Dashboard Fix: Drag-and-Drop to Preview Script (`dashboard.js`)

To fix your builder layout connection, the "Play Sample" button must directly examine what options are active on your drag-and-drop builder node elements when clicked.

Use this custom DOM-scraping logic block for your dashboard preview element:

```javascript
const previewCallBtn = document.querySelector('#play-sample-btn'); // Double check your dashboard play button ID

if (previewCallBtn) {
    previewCallBtn.addEventListener('click', async () => {
        try {
            // STEP A: Authorize browser mic input track instantly
            await navigator.mediaDevices.getUserMedia({ audio: true });

            // STEP B: Scrape your canvas node fields directly to grab user creations
            // Update these selector strings to match the exact input text fields inside your canvas cards
            const canvasLangSelector = document.querySelector('.node-language-input select, #canvas-lang-dropdown');
            const canvasVoiceSelector = document.querySelector('.node-voice-input select, #canvas-voice-dropdown');
            const canvasDesignationInput = document.querySelector('.node-designation-input input, #canvas-role-input');

            const selectedLang = canvasLangSelector?.value || "en";
            const selectedVoice = canvasVoiceSelector?.value || "Maya";
            const systemDesignation = canvasDesignationInput?.value || "General AI Front Desk Receptionist";

            const langConfig = HARKLY_LANGUAGES[selectedLang] || HARKLY_LANGUAGES["en"];
            const voiceId = HARKLY_VOICES[selectedVoice] || HARKLY_VOICES["Maya"];

            // STEP C: Assemble structural override payload alongside your dynamic designation prompt
            const assistantOverrides = {
                transcriber: {
                    provider: "deepgram",
                    model: "nova-2",
                    language: langConfig.transcriber
                },
                model: {
                    provider: "google",
                    model: "gemini-2.5-flash",
                    messages: [
                        {
                            role: "system",
                            content: `You are a professional voice receptionist working explicitly as a: ${systemDesignation}. Always communicate clearly, answer questions directly, and handle data collection logically.`
                        }
                    ]
                },
                voice: {
                    provider: "cartesia",
                    model: "sonic-multilingual",
                    voiceId: voiceId,
                    language: langConfig.voiceLang
                }
            };

            console.log("[Harkly Dashboard Sync] Triggering Canvas Agent configuration pipeline...");
            previewCallBtn.innerText = "Streaming...";

            // Launch call with Dashboard Master Canvas Assistant ID
            vapi.start("d5f28a96-25da-4905-bac8-5dee52a15f4e", assistantOverrides);

        } catch (err) {
            console.error("[Dashboard WebRTC Failure]:", err.message);
            previewCallBtn.innerText = "Play Sample";
        }
    });
}

```

---

## 📅 3. The Vapi Google Calendar Integration Check

If your agent is running tool operations but appointments aren't dropping onto your calendar layouts, your server response isn't communicating with Vapi correctly. Vapi requires a clear, explicit confirmation string structure passed back during a call stream to continue speaking naturally.

Ensure your backend `book_appointment_calendar` response routine format inside Replit looks exactly like this snippet:

```javascript
// Ensure your webhook route answers Vapi's system query layout inside 3 seconds
if (functionName === 'book_appointment_calendar') {
    // 1. Process your background logic tracking inputs safely
    const { customer_name, appointment_date, appointment_time } = args;
    
    console.log(`[Calendar Automation Active] Processing for ${customer_name}`);

    // 2. Return this EXACT structure. Vapi reads the 'result' value to tell the user the slot is locked
    return res.status(200).json({
        results: [
            {
                toolCallId: toolCallId, // Crucial: You MUST return the exact ID Vapi sent you
                result: `Successfully confirmed. I have scheduled an appointment on the calendar for ${customer_name} on ${appointment_date} at ${appointment_time}.`
            }
        ]
    });
}

```

---

### 🚀 What to Do Next

1. Paste these updated script blocks into your respective Replit workspace files.
2. Ensure that your HTML dropdown selectors match the exact strings used in the mapping tables (`en`, `hi`, `Maya`, `Arjun`).
3. Refresh your staging application page and run a fresh test session. Your connection will trigger instantly, and your selected voice tracks will speak back to you flawlessly in their target language formats.

Let me know how these updates perform. **Are your homepage dropdown controls updating the voice identity immediately now, or do we need to inspect your canvas card selector class names to lock down the drag-and-drop link?**