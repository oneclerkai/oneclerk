modules = ["nodejs-20", "web", "python-3.12"]
[agent]
expertMode = true
stack = "BEST_EFFORT_FALLBACK"

[nix]
channel = "stable-25_05"
packages = ["cargo", "libiconv", "libxcrypt", "openssl", "pkg-config", "postgresql", "rustc"]

[workflows]
runButton = "Project"

[[workflows.workflow]]
name = "Project"
mode = "parallel"
author = "agent"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "Start application"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "Frontend (Next.js)"

[[workflows.workflow]]
name = "Start application"
author = "agent"

[workflows.workflow.metadata]
outputType = "webview"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "cd oneclerk && uv run python -m uvicorn app.main:app --host 0.0.0.0 --port 5000 --reload"
waitForPort = 5000

[[workflows.workflow]]
name = "Frontend (Next.js)"
author = "agent"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "cd oneclerk/frontend && npm run dev"
waitForPort = 3000

[workflows.workflow.metadata]
outputType = "console"

[[ports]]
localPort = 3000
externalPort = 3001

[[ports]]
localPort = 5000
externalPort = 80

[deployment]
deploymentTarget = "autoscale"
run = ["bash", "-c", "cd oneclerk && uvicorn app.main:app --host 0.0.0.0 --port 5000"]

[userenv]

[userenv.shared]
ENVIRONMENT = "development"
JWT_SECRET_KEY = "replit-oneclerk-jwt-secret-key-2024"
OPENAI_MODEL = "gpt-4o-mini"
ELEVENLABS_MODEL = "eleven_turbo_v2_5"
