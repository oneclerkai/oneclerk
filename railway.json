{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python -m uvicorn oneclerk.app.main:app --host 0.0.0.0 --port $PORT",
    "healthCheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
